PORT=8131
NODE_ENV=development

DATABASE_URL="postgresql://sobogd@localhost:5432/iq_rest"

# Local-dev origins (CORS_PATTERN below covers prod *.iq-rest.com automatically).
CORS_ORIGINS=http://localhost:8132,http://192.168.1.142:8132
# Regex applied in addition to CORS_ORIGINS. Default: any *.iq-rest.com (and apex), http or https.
CORS_PATTERN=^https?://([a-z0-9-]+\.)?iq-rest\.com(:\d+)?$
